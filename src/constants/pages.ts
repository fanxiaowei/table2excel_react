export enum PAGES {
    ROOT = "/",
    HOME = "/home",
    SETTINGS = "/settings",
}
export enum OS {
    WP = 'wp',
    ANDROID = 'android',
    IOS = 'ios',
    UNKNOWN = 'unknown',
    WINDOWS = 'windows',
    MAC = 'mac',
    LINUX = 'linux',
}